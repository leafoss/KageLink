[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "kage-combat-lab"
version = "0.1.0"
description = "Deterministic 64px grid combat laboratory for KageLink"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
